import React from 'react';
export default function RightPanel() {
  return <div className="w-80 bg-panel border-l border-border shrink-0 flex items-center justify-center text-muted text-xs">Select a neighbourhood</div>;
}
